<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Kejian.TV API 客户端示例</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="This is a sample app demonstrating what we can do with Wendao API">
    <meta name="author" content="P.S.V.R">

    <!-- Le styles -->
    <link href="<%= u 'bootstrap.css' %>" rel="stylesheet">
    <link href="<%= u 'bootstrap-responsive.css' %>" rel="stylesheet">
    <link href="<%= u 'docs.css' %>" rel="stylesheet">
    <link href="<%= u 'prettify.css'%>" rel="stylesheet">

    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <link href="<%= u 'bootstrap-responsive.css' %>" rel="stylesheet">

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="<%= u 'favicon.ico' %>">
  </head>

  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Kejian.TV API 客户端示例</a>
          <div class="nav-collapse">
            <ul class="nav">
              <% tabs.keys.each do |tab_name| %>
                <%= tab tab_name %>
              <% end %>
            </ul>
            <% if session[:access_token] %>
              <p class="navbar-text pull-right">已获取访问令牌 ，登录为 <a href="#">username</a>. <a href="/exit">[退出]</a></p>
            <% else %>
              <p class="navbar-text pull-right">未获取访问令牌，<a href="<%= SERVER_endpoint %>/oauth/authorization?response_type=code&scope=all&client_id=<%= CLIENT_client_id %>&redirect_uri=<%= CLIENT_redirect %>">用wendao账号登录</a></p>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">

      <!-- Masthead
      ================================================== -->
      <header class="jumbotron subhead" id="overview">
        <h1>Wendao API</h1>
        <p class="lead">这个应用程序是一个用Sinatra开发的使用Kejian.TVAPI的示例，基于Ruby客户端库wendao(<a href="http://rubygems.org/gems/wendao">http://rubygems.org/gems/wendao</a>)。
        </p>
      </header>

      <%= yield %>
      
      <!-- Footer
      ================================================== -->
      <footer class="footer">
        <p class="pull-right"><a href="#">Back to top</a></p>
        <p>Kejian.TVAPI客户端示例由<a href="http://ofpsvr.org">P.S.V.R</a>实现和维护，<a href="https://github.com/pmq20/wendao">源代码</a>在MIT协议下发布.</p>
        <p><a href="http://wendao.zhaopin.com">Kejian.TV</a>为<a href="http://zhaopin.com">智联招聘</a>旗下产品，智联招聘版权所有.</p>
      </footer>
    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="<%= u 'jquery-1.7.2.min.js' %>"></script>
    <script src="<%= u 'bootstrap.min.js' %>"></script>
    <script type="text/javascript" charset="utf-8">
    $(document).ready(function(){
      $('button:not(.special)').click(function(){
        window.location.href = $(this).attr('href');
      })
    });
    </script>
    
  </body>
</html>
